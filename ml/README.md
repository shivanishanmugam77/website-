# ml/

Populated from Phase 4.

* `models/`      downloaded weights (git-ignored)
* `scripts/`     model download and export scripts
* `evaluation/`  benchmark harness and metrics against labelled catalogue pages
* `notebooks/`   exploration only; nothing here is imported by the application

Model choices and licensing notes are part of the Phase 0 architecture document.
