"""ML provider interfaces and implementations.

Populated from Phase 4 (OCR) and Phase 5 (detection, segmentation). Nothing in
this package is imported by the API process; only worker processes load models.
"""
